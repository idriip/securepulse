[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]name: SecurePulse Security Pipeline

brew tap hashicorp/tap
brew install hashicorp/tap/terraform