apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: securepulse
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: securepulse
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s